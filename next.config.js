/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.datocms-assets.com',
      'images.yapily.com',
      'countryflagsapi.com',
    ],
  },
};

module.exports = nextConfig;
